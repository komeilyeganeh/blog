import type { FC } from "react";

export const Logo: FC = () => {
    // :::: return JSX ::::
    return <div>
        <h2 className="uppercase text-3xl font-bold text-slate-800 italic">blog</h2>
    </div>
}